import { RoomAdditionalServices } from './room-additional-services';

describe('RoomAdditionalServices', () => {
  it('should create an instance', () => {
    expect(new RoomAdditionalServices()).toBeTruthy();
  });
});
