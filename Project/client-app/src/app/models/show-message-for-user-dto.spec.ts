import { ShowMessageForUserDTO } from './show-message-for-user-dto';

describe('ShowMessageForUserDTO', () => {
  it('should create an instance', () => {
    expect(new ShowMessageForUserDTO()).toBeTruthy();
  });
});
